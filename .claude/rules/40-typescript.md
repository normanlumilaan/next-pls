---
paths:
  - "**/*.{ts,tsx}"
---

# TypeScript conventions

- Prefer existing patterns in this codebase.
- Keep a single source of truth for derived values.
- Prefer pure functions in `lib/*` for business logic.
- Utility naming: `verb-namespace-detail`.

