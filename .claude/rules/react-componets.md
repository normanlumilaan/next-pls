# React components

## Machine-friendly guidance

- **JSDoc scope**: required for any exported component (named or default export).
- **What to include**:
  - One-line purpose (“why it exists”)
  - Props summary (or state “no props”)
  - Important constraints (e.g., Client Component requirements)

If a component is render-only and takes no props, a short JSDoc is still required.
