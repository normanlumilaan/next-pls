---
paths:
  - "**/*.tsx"
---

# React components

- Follow the 3-layer architecture rule when business logic is involved.
- Exported components must meet the JSDoc rule (purpose + params explained).
- Prefer small render-only components and custom hooks over monolith components.
- Presentational “card” components live in `components/cards/`.

